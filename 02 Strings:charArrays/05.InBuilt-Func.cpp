#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    // char array1[100];
    // cin.getline(array1, 100);
    // char array2[100];
    // cin.getline(array2, 100);

    //  -->  length :-    "
    // int len = strlen(array1);
    // cout << "Array length = " << len << endl;

    //  -->  compare:-   not returns bool values
    // if (strcmp(array1, array2))
    // {
    //     cout << "Not Equal" << endl;
    // }
    // else
    // {
    //     cout << "Both Equal" << endl;
    // }

    //  -->  strcpy :-  to copy from 1 string to another , it copies '\0' character too
    // char a[100];
    // strcpy(a, array1);
    // cout << "Value copied to a : " << a << endl;

    // -->  strncpy :-  to copy first n characters,it don't copies '\0' character
    // char c[100];
    // strncpy(c, array1, 3);
    // cout << "Strncpy output after coping 3 elements of array 1 : " << c << endl;

    // --> strcat :- concatenates str2 after str1
    // char str1[100] = "abcd";
    // char str2[100] = "efg";
    // strcat(str1, str2);
    // cout << "strcat on str1 of str2 : " << str1 << endl;

    return 0;
}